#!/usr/bin/env bash
# --- Git settings ---
echo "--- Git settings ---"
git config --global user.name "Berry Yogurt"
git config --global user.email berryogurt@outlook.com
git config --global init.defaultBranch main
git config --global core.editor "nvim"
